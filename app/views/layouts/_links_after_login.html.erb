<% if user_signed_in? %>
  <%= link_to t("views.logout"), destroy_user_session_path, method: :delete %>
<% end %>

<% if user_signed_in? && current_user == @user %>
  <%= link_to t("views.user_edit"), edit_user_registration_path %>
<% end %>

<%= link_to t("views.user_index"), users_path %>